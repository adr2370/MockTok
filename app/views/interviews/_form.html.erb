<%= form_for(@interview) do |f| %>
  <% if @interview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@interview.errors.count, "error") %> prohibited this interview from being saved:</h2>

      <ul>
      <% @interview.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :expected_time %><br />
    <%= f.number_field :expected_time %>
  </div>

  <%= f.hidden_field :waiting, :value => true %>

    <br><%= radio_button_tag 'role', 'interviewer', true %> Interviewer
    <br><%= radio_button_tag 'role', 'interviewee' %> Interviewee

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
